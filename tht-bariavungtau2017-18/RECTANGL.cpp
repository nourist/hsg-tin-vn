#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ld long double

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	if(fopen("RECTANGL.inp", "r")) {
		freopen("RECTANGL.inp", "r", stdin);
		freopen("RECTANGL.out", "w", stdout);
	}

	int n, x, y;
	cin >> n >> x >> y;

	int t = 1, b = n, l = 1, r = n;
	while (1) {
		
	}
}
