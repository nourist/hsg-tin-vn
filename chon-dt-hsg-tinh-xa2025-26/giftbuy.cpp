#include <bits/stdc++.h>
using namespace std;

#define FOR(i,l,r) for (int i = l; i <= r; ++i)
#define FOD(i,r,l) for (int i = r; i >= l; --i)
#define ll long long
#define ld long double

int a[(int)1e5 + 5];

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	if (fopen("giftbuy.inp", "r")) {
		freopen("giftbuy.inp", "r", stdin);
		freopen("giftbuy.out", "w", stdout);
	}

	int n, k;
	cin >> n >> k;
	FOR(i, 1, n) cin >> a[i];


}
